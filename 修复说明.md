# API连接问题修复说明

## 问题描述
移动端Flutter应用在获取传感器数据时失败：`type '_ Map < String , dynamic >' is not a subtype of type ' List < dynamic >'`

## 问题原因
Flutter代码在 `app/lib/services/api_service.dart` 中有变量重复声明的问题

## 已完成的修复

### 1. 后端API修复 ✅
- `/api/sensor-data` 现在返回格式：`{"data": [...]}`
- 所有字段使用下划线命名 (snake_case)
- 所有字段类型正确

### 2. Flutter代码修复 ✅
文件：`app/lib/services/api_service.dart`

```dart
// 修复前（错误）
if (response.statusCode == 200) {
  final List<dynamic> data = response.data;
  return data.map((e) => SensorData.fromJson(e)).toList();
}

// 修复后（正确）
if (response.statusCode == 200) {
  dynamic responseData = response.data;
  if (responseData is Map<String, dynamic>) {
    responseData = responseData['data']; // 智能提取data字段
  }
  final List<dynamic> dataList = responseData;
  return dataList.map((e) => SensorData.fromJson(e)).toList();
}
```

## 需要用户操作

### 方式1：在Flutter开发环境重新编译

```bash
cd /home/node/.openclaw/workspace/fishfarm-system/app

# 清理缓存
flutter clean

# 获取依赖
flutter pub get

# 重新编译
flutter run
```

### 方式2：在Android Studio中重新运行

1. 打开 `/home/node/.openclaw/workspace/fishfarm-system/app`
2. 点击 "Run" 或按 Shift+F10

### 方式3：直接运行APK

如果已有编译好的APK：
```bash
cd /home/node/.openclaw/workspace/fishfarm-system/app
flutter build apk --release
adb install app-release.apk
```

## API测试

确认后端正常运行：

```bash
# 健康检查
curl http://192.168.1.200:8080/health

# 获取传感器数据
curl http://192.168.1.200:8080/api/sensor-data
```

期望返回：
```json
{
  "data": [
    {
      "id": 1,
      "device_id": 1,
      "device_name": "水温传感器1",
      "temperature": 25.5,
      "ph": 7.2,
      ...
    }
  ]
}
```

## 验证修复

重新编译运行后：
1. 打开应用
2. 进入仪表盘页面
3. 查看传感器数据是否正常加载
4. 应该可以看到温度、PH值、溶解氧等数据

## 技术细节

### 智能格式处理
Flutter代码现在可以自动处理两种返回格式：
- `{"data": [...]}` - 提取data字段
- `[...]` - 直接使用数组

### 字段命名规范
所有字段使用下划线命名（snake_case）：
- device_id 而不是 deviceId
- device_name 而不是 deviceName
- created_at 而不是 createdAt

### 字段类型规范
- id: int
- device_id: int
- device_name: String
- temperature: double
- ph: double
- ...
