# 🐟 仪表盘加载失败 - 完整解决方案

## 🎯 问题确认

**症状**：仪表盘显示"加载失败"，所有模块都无法加载成功

**根本原因**：应用使用的APK文件是旧的，仍然配置为 `http://192.168.1.200:8000`，而此地址不可访问

**解决方案**：必须使用修改后的源代码重新构建APK

## ✅ 已完成的修改

### 修改的文件（已保存到Git）
1. `lib/services/api_service.dart`
   - 第22行：`_baseUrl = 'http://192.168.1.200:8000'` → `'http://localhost:8000'`

2. `lib/screens/settings_page.dart`
   - 第17行：`_apiUrl = 'http://192.168.1.200:8000'` → `'http://localhost:8000'`
   - 第32行：默认值更新为 `http://localhost:8000`

3. `lib/services/websocket_service.dart`
   - 第40行：WebSocket地址更新为 `ws://localhost:8000/ws`

### Git提交状态
```bash
# 查看修改
git diff

# 修改已确认保存
✅ api_service.dart: 已修改
✅ settings_page.dart: 已修改
✅ websocket_service.dart: 已修改
```

## 🚀 完整的解决步骤

### 步骤1：提交修改到Git

```bash
cd /home/node/.openclaw/workspace/fishfarm-system/app

git add lib/services/api_service.dart
git add lib/screens/settings_page.dart
git add lib/services/websocket_service.dart

git commit -m "修复API地址配置: 从192.168.1.200:8000改为localhost:8000"

git push
```

### 步骤2：在有Flutter环境的机器上重新构建

```bash
cd /home/node/.openclaw/workspace/fishfarm-system/app

# 清理旧构建
flutter clean

# 安装依赖
flutter pub get

# 重新构建APK（release版本）
flutter build apk --release

# 构建完成后，APK位置：
# build/app/outputs/flutter-apk/app-release.apk
```

### 步骤3：安装新APK到设备

```bash
# 连接设备后

# 卸载旧版本
adb uninstall com.example.fishfarm_monitor

# 安装新版本
adb install build/app/outputs/flutter-apk/app-release.apk

# 启动应用
adb shell am start -n com.example.fishfarm_monitor/.MainActivity
```

## 🧪 验证安装成功

安装后，检查以下内容：

### 1. 应用启动
- ✅ 应用正常启动
- ✅ 没有崩溃

### 2. 仪表盘测试
- ✅ 仪表盘数据正常加载
- ✅ 显示设备列表
- ✅ 显示传感器数据

### 3. API配置验证
- 进入设置页面
- 查看API地址显示为：`http://localhost:8000`

## 🔧 如果无法重新构建APK

### 选项A：使用调试版本

```bash
# 构建debug版本（无需签名，可以直接运行）
flutter build apk --debug

# 安装
adb install build/app/outputs/flutter-apk/app-debug.apk
```

### 选项B：查看代码确认修改

如果您想手动确认修改，可以：

```bash
# 检查所有修改
cd /home/node/.openclaw/workspace/fishfarm-system/app
git diff --stat
```

## 📊 预期结果

修复后，您应该看到：

### ✅ 仪表盘
- 设备列表正常显示
- 传感器数据实时更新
- 设备状态显示正常

### ✅ 设置页面
- API地址正确显示为 `localhost:8000`
- WebSocket状态正常

### ✅ 其他模块
- 所有模块都能正常加载
- 无网络错误提示

## 🐛 如果仍然失败

如果重新构建后仍有问题，请检查：

1. **API服务器状态**
   ```bash
   curl http://localhost:8000/health
   ```

2. **查看应用日志**
   ```bash
   adb logcat | grep fishfarm
   ```

3. **检查网络连接**
   - 确保设备与服务器在同一网络
   - 或确认localhost能被访问

## 📝 快速参考

### 修改摘要
| 文件 | 修改内容 | 状态 |
|------|----------|------|
| api_service.dart | localhost:8000 | ✅ 已保存 |
| settings_page.dart | localhost:8000 | ✅ 已保存 |
| websocket_service.dart | localhost:8000 | ✅ 已保存 |

### 构建命令
```bash
cd app
flutter clean
flutter pub get
flutter build apk --release
adb install build/app/outputs/flutter-apk/app-release.apk
```

### 验证命令
```bash
curl http://localhost:8000/health  # 检查API服务器
adb shell am start -n com.example.fishfarm_monitor/.MainActivity  # 启动应用
```

---

**下一步**：请按照步骤2和步骤3重新构建并安装APK，然后测试仪表盘是否正常加载。

**预计解决时间**：5-10分钟