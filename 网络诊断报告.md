# APP连接失败问题排查报告

**生成时间**: 2026-03-31 09:15  
**问题**: APP连接局域网IP和公网IP的8080端口都失败

---

## 🔍 问题分析

### 1. API服务器状态
- ✅ **服务器运行正常** - 端口8080已启动
- ✅ **健康检查通过** - `/health` 端点响应正常
- ✅ **数据接口正常** - `/api/devices` 等端点工作正常

### 2. 网络配置问题

**发现的关键问题：**

#### ❌ IP地址不匹配
- **APP默认配置**: `http://192.168.1.200:8080`
- **您提供的IP**: `192.168.1.100:8080`
- **容器内部IP**: `172.17.0.2:8080`

#### ❌ 运行环境问题
- API服务器运行在**Docker容器内**
- 容器内部IP: `172.17.0.2`
- 需要通过**端口映射**或**主机网络模式**才能从外部访问

---

## 💡 解决方案

### 方案A：在APP中修改API地址（推荐）

APP已支持动态配置API地址，请按以下步骤操作：

1. **打开APP**
2. **进入"设置"页面**
3. **找到"API地址"设置项**
4. **修改为正确的地址**：
   - 局域网访问：`http://您的局域网IP:8080`
   - 公网访问：`http://112.64.186.254:8080`
5. **点击"测试连接"按钮**
6. **保存设置**

### 方案B：配置端口映射

如果您的系统运行在Docker容器中，需要配置端口映射：

```bash
# 检查当前的端口映射
netstat -tlnp | grep 8080

# 或者使用Docker的端口映射
# 确保容器启动时使用了 -p 8080:8080 参数
```

### 方案C：使用主机网络模式

如果需要让服务器直接监听在主机网络上：

1. 停止当前容器
2. 使用 `--network host` 参数重新启动容器
3. 或者直接在主机上运行API服务器（不使用容器）

---

## 🧪 测试步骤

### 1. 确认服务器运行

```bash
# 在服务器上测试
curl http://localhost:8080/health
# 应该返回: {"status": "healthy", "service": "fishfarm-api"}
```

### 2. 确认正确的局域网IP

```bash
# 查看网络接口
hostname -I
# 或者
ifconfig
```

### 3. 从其他设备测试

```bash
# 从同一局域网的其他设备测试
curl http://[您的局域网IP]:8080/health
```

---

## 📝 APP配置文件位置

如果需要修改APP的默认配置：

- **主API服务**: `app/lib/services/api_service.dart`
- **WebSocket服务**: `app/lib/services/websocket_service.dart`
- **设置页面**: `app/lib/screens/settings_page.dart`

修改后需要重新编译APK。

---

## ✅ 快速验证清单

- [ ] 确认API服务器正在运行
- [ ] 确认正确的局域网IP地址
- [ ] 确认端口8080未被防火墙阻止
- [ ] 在APP设置中修改API地址
- [ ] 测试连接是否成功

---

**建议优先使用方案A**（在APP设置中修改API地址），这是最简单快捷的解决方法。
